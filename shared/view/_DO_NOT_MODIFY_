Views are generated automatically.

If you want to change them, just change the template folder.

In this way, templates are highlighted as HTML files instead of JS strings.

But of course in your own project you can do whatever you prefer.